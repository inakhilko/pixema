import Form from '../../components/Form';
import { signInPageData } from '../../constants/forms';

function SignInPage() {
  const
  return <Form formInfo={signInPageData} onSubmit={(data)=>{}} staticMessage={} />;
}

export default SignInPage;
